import { SimpleChangeDirective } from './simple-change.directive';

describe('SimpleChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleChangeDirective();
    expect(directive).toBeTruthy();
  });
});
